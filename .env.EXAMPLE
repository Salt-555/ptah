# Ptah needs no API keys in .env — reviewer auth rides your Nous Portal
# OAuth (hermes login --provider nous). Add provider keys here only if you
# point config.yaml's model section at a key-based provider.
