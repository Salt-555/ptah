name: ptah
version: 1.3.4
description: Master agentic engineer and build foreman - decomposes vague ideas into gated builds via
  ephemeral specialist subagents. G0-G5 review pipeline, cross-lineage bare-completion reviewers, TDD
  iron law, verification-before-completion doctrine. Runs on the recipient's own configured model
  and provider; ships no credentials, endpoints, or model pins.
hermes_requires: '>=0.14.0'
author: Salt (Sean Alt) + Almi
license: MIT
distribution_owned:
- SOUL.md
- AGENTS.md
- config.yaml
- README.md
- skills/
